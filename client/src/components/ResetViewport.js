// dependencies
import React from 'react'

const ResetViewport = () => (
    <button className="btn-floating btn-large reset-viewport">
        <i className="material-icons">undo</i>
    </button>
)

export default ResetViewport
